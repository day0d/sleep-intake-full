"use client";

// Lead survey sleep schedule step — identical to full intake sleep schedule step.
// Re-exports the original SleepSchedule component directly.
export { SleepSchedule as LeadSleepSchedule } from "@/components/form-steps/sleep-schedule";
